<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <display-name>Product Catalog Service</display-name>

    <!-- Инициализация кастомного ApplicationContext -->
    <context-param>
        <param-name>applicationContextClass</param-name>
        <param-value>org.example.context.ApplicationContext</param-value>
    </context-param>

    <!-- Активация AspectJ через load-time weaving -->
    <context-param>
        <param-name>aspectj.weaving</param-name>
        <param-value>on</param-value>
    </context-param>

    <!-- Конфигурация для AspectJ -->
    <context-param>
        <param-name>org.aspectj.weaver.loadtime.configuration</param-name>
        <param-value>classpath:aop.xml</param-value>
    </context-param>

</web-app>